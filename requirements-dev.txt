# Development and CI dependencies (not needed at runtime).
# Installed in CI to run the test suite under pytest with coverage, producing
# the coverage.xml the SonarCloud quality gate consumes.
# Pinned exactly (githubactions:S8544): CI resolves these versions today;
# bump deliberately, not implicitly.
pytest==9.1.1
pytest-cov==7.1.0
